# Postgres connection (used by Prisma migrate + the pg driver adapter at runtime)
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/betabet?schema=public"

# Auth.js — generate a secret with: npx auth secret  (or: openssl rand -base64 32)
AUTH_SECRET=""

# Google OAuth credentials (https://console.cloud.google.com/apis/credentials)
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""
